# Cloned OpenFOAM source (large, not committed)
/openfoam/

# Cloned OpenFOAM build artifacts (in external source tree)
# dist/ is committed so GitHub Pages can deploy it

# OS / editor
.DS_Store
*.swp
*~
